## 🧠 タスク06：ツール運用フローのシミュレーション結果

### フェーズ1：KGI（最終ゴール）の決定
**[使用ツール：`2.prop.md` (If-Thenシナリオ) → `共創ワークショップシート_例.md` (C-1)]**

1.  **現状認識（As-Is）の提示:**
    * まず、メインプレゼン（`2.prop.md`）で「下期 営業利益率 ▲39.8%」という財務ファクトを提示します。
2.  **成功イメージ（方向性）の選択:**
    * `C-1`ワークショップシート（`共創ワークショップシート_例.md`）のセクション1（成功のイメージ）を提示します。
    * ここで、メインプレゼンで提示したIf-Thenシナリオ（A:理念, B:効率, C:拡張）を再度見せ、「下期の赤字を止めるために、まずどの方向性を優先すべきか？」を問いかけます。
    * *（シミュレーション）:* クライアントは「まずはB（効率・成果）で足元の赤字を止める」を選択します。
3.  **KGI（1本）の確定:**
    * 方向性が「B（効率・成果）」に決まったため、`C-1`のセクション2（KGI）に「**下期 営業利益率**」をKGIとして記入します。
4.  **KGI目標値の設定:**
    * `KPI_インパクトテーブル.md`のサマリー（B方向：営利率 +3〜5pt）を根拠に、目標値を設定します。
    * *（計算例）:* 現状（▲39.8%）+ 改善幅（例: +4.8pt） = **目標「▲35.0%」** 。

### フェーズ2：Top1レバー（最優先手段）の決定
**[使用ツール：`C-2` (5大ハンドル) → `task_4.md`, `task_5.md` (翻訳スクリプト)]**

5.  **Top1レバーの議論:**
    * KGI（営利率+4.8pt）が決まったことを受け、`C-2`ワークショップシート（`共創ワークショップシート_例.md`）に移ります。
    * 「では、このKGIを達成するために、5つのハンドルのうち**どれを最優先で回すべきか**？」と問いかけます。
6.  **選定根拠の説明（タスク05）:**
    * ここで`task_5.md`のスクリプトを使い、「なぜこの5つか」のロジック（PLインパクトと独自アセット）を説明します。
7.  **各ハンドルの翻訳（タスク04）:**
    * `task_4.md`の翻訳スクリプトを用い、特に「② 人材」について、「B（効率）」の選択がKGIに直結することを強調します（例：「『販管費の88%』を占める最重要論点です。『**委託費/売上比**』の追求が即座に利益率（KGI）に直結します」）。
8.  **Top1レバーの決定:**
    * *（シミュレーション）:* なーすけ氏が、KGIへのインパクトが最大である「**② 人材**」をTop1レバー（★★★）として選択します。
    * `C-2`シートの「根拠」欄に「KGI直結」と明記します。

### フェーズ3：KPI（先行指標）への翻訳と論理的整合
**[使用ツール：`KPI_インパクトテーブル.md` (D-1) → `C-1` (KPI記入)]**

9.  **Top1 KPIの選定:**
    * 再び`C-1`ワークショップシートのセクション3（KPI）に戻ります。
    * 「Top1レバー（人材）を回すことでKGI（営利率）を達成するKPIは何か？」と問いかけ、`KPI_インパクトテーブル.md`（D-1）を提示します。
10. **インパクトの提示（D-1）:**
    * D-1の「委託費/売上比」の行を指し、「**委託費/売上比 ▲5% → 営利率 +約4pt 寄与**」というロジックを読み上げます。
11. **KPIの確定と論理整合:**
    * `C-1`に**KPI #1「委託費/売上比」**を記入。現状（25%）、目標（20%）と設定します。
    * ここで「整合チェック」を行います：「KGI目標（+4.8pt）に対し、KPI #1で（+4pt）の寄与が見込めました。**ロジックが整合しています**」と宣言します。
12. **その他KPIの確定（タスク07の実行）:**
    * A案（理念）の仕込みとして必要なKPI（例：入会率、継続率）も`C-1`に記入します。
    * 天池氏に現状値を聞き、「**不明**」との回答を得ます。
    * `タスク 07`のスクリプトを実行：「ありがとうございます。**『不明である』とわかったことが今日の成果です。**まずはこれを『可視化』することを目標としましょう」。

### フェーズ4：決定事項の集約（DoDの達成）
**[使用ツール：`C-1` (Next Action) → `決定DoDチェックカード_例.md` (転記・クロージング)]**

13. **Next Actionの確定:**
    * `C-1`のセクション5（Next Actions）に移り、「KPI #1（委託費）の達成」と「KPI #3, #4（CVR等）の可視化」のために「まず何をすべきか」を議論し、Ownerと期日を確定させます。
14. **DoDカードへの転記:**
    * 最後に`決定DoDチェックカード_例.md`を取り出します。
    * 「議論が発散しないよう、**今決まったこと**をこのカードに転記します」と宣言し、`C-1` `C-2`からKGI, KPI, Top1レバー, Next Actionを書き写します。
15. **クロージング（A案着手）:**
    * `決定DoDチェックカード_例.md`のセクション2（A案の着手可否）を指します。
    * 「本日決定したNext Action（特にKPIの可視化）こそが、ご提案した**A案（経営基盤プラン）**の最初のステップそのものです。本日から着手させていただけますか？」と問いかけ、クロージングします。